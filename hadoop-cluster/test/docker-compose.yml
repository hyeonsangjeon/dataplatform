version: "2"

services:
  master:
    image: modenaf360/hadoop-base:latest
    container_name: master
    hostname: master
    volumes:
      - ./hadoop_master:/hadoop/dfs/name
    environment:
      - CLUSTER_NAME=test
    env_file:
      - ./hadoop.env

